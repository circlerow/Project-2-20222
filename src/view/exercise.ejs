<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.4/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/exercise.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>Exercise</title>
</head>

<body>
    <div class="w3-container">
        <h2>Danh sách bài tập</h2>
        <table class=" table table-striped-columns">
            <thead class="table-primary">
                <tr class="">
                    <th>Bài tập </th>
                    <th>Tên bài</th>
                    <th>Độ khó</th>
                    <th>Giải bài tập</th>
                </tr>
            </thead>
            <tbody class="table-warning">
                <% for(let i=0; i < dataExercise.length; i++) { %>
                    <tr>
                        <td>
                            <%= dataExercise[i].Id %>
                        </td>
                        <td>
                            <%= dataExercise[i].content %>
                        </td>
                        <% if(dataExercise[i].level=='Easy' ){ %>
                            <td class="green">
                                <%= dataExercise[i].level %>
                            </td>
                            <% } else if(dataExercise[i].level=='Medium' ){ %>
                                <td class="yellow">
                                    <%= dataExercise[i].level %>
                                </td>
                                <% } else if(dataExercise[i].level=='Hard' ){ %>
                                    <td class="red">
                                        <%= dataExercise[i].level %>
                                    </td>
                                    <% } %>
                                        <td>
                                            <button type="button" class="btn btn-outline-primary"><a
                                                    href="/detail/exercise/<%= dataExercise[i].Id%>">Giải
                                                    bài tập số <%= dataExercise[i].Id%></a></button>
                                        </td>
                    </tr>
                    <% } %>
            </tbody>


        </table>
        <div class="d-grid gap-2 col-6 mx-auto">
            <button type="button" class="btn btn-primary"><a href="/index" class="text-light">Chuyển đến trình biên
                    dịch</a></button>
        </div>
    </div>
</body>

</html>